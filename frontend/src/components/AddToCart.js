/* import React from 'react';

const AddToCart = ({ product, addToCartHandler }) => {
  return (
    <button onClick={() => addToCartHandler(product)}>
      Add to Cart
    </button>
  );
};

export default AddToCart;
 */